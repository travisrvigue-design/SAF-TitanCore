Dataset: Guidance on datasets used for validation.

- Use synthetic or publicly available non-sensitive datasets for CI.
- Do not include human-identifiable data without approvals (IRB/HIPAA).
- All datasets referenced in benchmarks must have manifest entries with SHA-256 fingerprints and provenance metadata.
