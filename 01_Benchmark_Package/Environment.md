Environment: Python 3.11, Linux (Ubuntu 22.04+), optional GPU nodes for tensor workloads. All environments used for benchmarks must be documented with exact OS, kernel, CPU, memory, and installed packages with pinned versions. Use reproducible images (see /docker/Dockerfile).

Security: Do not store secrets in repo. Use GitHub Secrets or OIDC for signing and provider access.
