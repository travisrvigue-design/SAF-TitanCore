# Debugging guide

If CI still fails, download the pytest-report artifact from the failed run (Actions -> run -> Artifacts -> pytest-report).

You can also run `ci/run_tests.sh` locally to reproduce the CI steps.
